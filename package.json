{
  "name": "rule-guess-game",
  "version": "1.0.0",
  "private": true,
  "description": "Renderなどにデプロイする際、このルートpackage.jsonのbuild/startスクリプトが使われます。ローカル開発では引き続きserver/・client/それぞれで npm install / npm start / npm run dev を使ってください。",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18"
  }
}
